# Shapes
This file is written in Python 3.11

It implements storage (Saving and Loading) of objects through classes with the help of a Python library `pickle`
This task focuses on storing different subclasses of a class Shape, where each of the shapes have their own variables necessary to store
